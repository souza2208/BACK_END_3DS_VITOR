function sobradosalario(meusalario) {
    const contadeluz = 150.0
    const aluguel = 1400.0
    const resto = meusalario - (contadeluz+aluguel)
    console.log(`o que restou no fim do mes ${resto}`)
}